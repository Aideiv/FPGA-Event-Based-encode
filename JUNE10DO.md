# JUNE10DO — Company-Wide Commit Sprint

> **Goal:** Every team member gets meaningful, reviewable commits in this repo.
> **Repo:** [FPGA Event-Based Drone Collision Avoidance](https://github.com/Enotrium/FPGA-Event-Based-encode)
> **Date:** June 10, 2026

---

## Current State

| Metric | Value |
|--------|-------|
| **Total committers** | 1 (`Aidistides`) |
| **GitHub teams in CODEOWNERS** | 9 |
| **People needed** | 8 |
| **Total tasks below** | 29 discrete commits |
| **File conflicts** | 0 (each task touches only one person's CODEOWNERS domain) |

---

## Prerequisites

1. **Create GitHub teams** in the Enotrium org (if not already existing):
   - `@Enotrium/core`, `@Enotrium/fpga`, `@Enotrium/embedded`, `@Enotrium/autonomy`
   - `@Enotrium/ml`, `@Enotrium/qa`, `@Enotrium/devops`, `@Enotrium/docs`, `@Enotrium/security`, `@Enotrium/systems`
2. **Add people** to the appropriate teams.
3. **Ensure write access** to this repo for all team members.
4. **Branch naming convention:** `initials/june10-taskN` (e.g., `ay/june10-fpga-synth`)

---

## Task Breakdown by Individual

---

### Person 1 — FPGA Engineer (`@Enotrium/fpga`)

> **Owns:** `fpga/` (all HLS/C++ modules, `build.tcl`, `config.yaml`)
> **Difficulty:** 🔴 Hard | **Commits:** 4

| # | Task | Files | Description |
|---|------|-------|-------------|
| 1 | **Synthesize and report real resource numbers** | `fpga/build.tcl`, `fpga/README.md` | Run `vitis_hls -f build.tcl` on licensed hardware. Replace all theoretical LUT/DSP/BRAM/latency estimates in `fpga/README.md` and `PRODUCTION_READINESS.md` with actual post-synthesis numbers. |
| 2 | **Add AXI-Stream handshake to `aer_interface.h`** | `fpga/aer_interface.h` | Add `TVALID`/`TREADY` compliant handshake for the AER camera input to enable real hardware integration with Prophesee/Inivation event cameras. |
| 3 | **Pipeline depth annotation pass** | `fpga/*.h` | Add `#pragma HLS PIPELINE II=1` and `#pragma HLS DATAFLOW` directives throughout the 6-stage pipeline. Measure and document actual initiation interval. |
| 4 | **Write INT16 Q4.12 weight-loading test** | `fpga/testbench.cpp` | Extend testbench to load INT16 Q4.12 weights generated by `train/convert_weights_to_fpga.py --quantize-int16` and verify bit-exact match against golden model output. |

---

### Person 2 — Embedded/ARM Engineer (`@Enotrium/embedded`)

> **Owns:** `arm/` (all C++ controller code, `Makefile`)
> **Difficulty:** 🟠 Medium | **Commits:** 3

| # | Task | Files | Description |
|---|------|-------|-------------|
| 1 | **Integrate Kalman tracker into main loop** | `arm/drone_main.cpp`, `arm/kalman_tracker.h` | Wire the existing 4D Kalman filter header into `drone_main.cpp`. Currently it's an isolated header with no caller—add track initialization, predict/update cycle, and lifecycle management in the main control loop. |
| 2 | **Add MAVLink telemetry logging to SD** | `arm/mavlink_bridge.h`, `arm/drone_main.cpp` | Add `.bin` telemetry logging over SPI to SD card from the MAVLink v2 bridge. Essential for post-flight analysis and debugging. |
| 3 | **Benchmark and optimize control loop latency** | `arm/collision_predictor.h`, `arm/evasion_controller.h` | Profile the 100 Hz control loop. Identify hotspots. Add `-O3`/LTO flags to `arm/Makefile`. Document worst-case latency in header comments. |

---

### Person 3 — Autonomy/Drone Engineer (`@Enotrium/autonomy`)

> **Owns:** `drone/` (Python drone control package)
> **Difficulty:** 🟠 Medium | **Commits:** 3

| # | Task | Files | Description |
|---|------|-------|-------------|
| 1 | **Add closed-loop SITL integration test** | `drone/drone_controller.py` | Write a test that connects `drone_controller.py` to the HIL bridge (`test/hil_gazebo_bridge.py`) and runs a full 60-second obstacle course with assertions on evasion level transitions and collision outcomes. |
| 2 | **Tune evasion controller thresholds** | `drone/evasion_controller.py` | Run the HIL simulator across parameter sweeps (danger thresholds, hysteresis values, velocity gains). Produce a CSV/plot of response curves. Adjust constants. Commit the tuned values with before/after comparison. |
| 3 | **Add IMU bias calibration** | `drone/drone_controller.py` | Add a 5-second static calibration phase at boot that estimates gyro/accel bias via sample mean and feeds into sensor fusion. Reduces drift on ARM deployment. |

---

### Person 4 — ML Engineer (`@Enotrium/ml`)

> **Owns:** `models/`, `train/`, `fpga/config.yaml` (joint with fpga)
> **Difficulty:** 🔴 Hard | **Commits:** 4

| # | Task | Files | Description |
|---|------|-------|-------------|
| 1 | **d=384 → d=128 from-scratch retraining** | `train/s1_train.py`, `train/fpga_training_config.py` | Run `python train/s1_train.py --params FPGAParams` to produce a from-scratch d=128 model. Currently only weight truncation exists (`convert_weights_to_fpga.py`), not a properly trained d=128 model. Commit the checkpoint and AEPE metrics. |
| 2 | **Benchmark ensemble vs single-pass accuracy** | `models/estimator.py`, `train/s2_inference.py` | Full evaluation comparing `ensemble=3` vs `ensemble=1` across all 5 datasets (UNION, MVSEC, DSEC, EVIMO, FPV). Produce an AEPE comparison table. Commit results to `train/README.md`. |
| 3 | **Export ONNX model for Vitis AI DPU** | `train/convert_weights_to_fpga.py` | The Bonazzi CNN pipeline (Pipeline 2) has no ONNX export. Add `--export-onnx` flag to convert the Python CNN to ONNX format for Vitis AI DPU compilation. |
| 4 | **Add adversarial robustness evaluation** | `test/golden_model_test.py` or new `test/adversarial_test.py` | `SECURITY.md` lists adversarial robustness as a concern. Add FGSM/PGD attack tests on the flow estimator. Report the robustness-accuracy tradeoff curve. |

---

### Person 5 — QA/Test Engineer (`@Enotrium/qa`)

> **Owns:** `test/` (all test files)
> **Difficulty:** 🟡 Easy-Medium | **Commits:** 4

| # | Task | Files | Description |
|---|------|-------|-------------|
| 1 | **Add regression assertions for all 5 sim scenarios** | `test/test_fpga_simulator.py` | Currently the 5 scenarios (looming, lateral, noise, multi-object, throughput) are visual-only. Add pass/fail assertions: danger level transitions, TTC monotonicity, evasion vector direction, throughput floor. |
| 2 | **Add fuzzing tests for edge-case inputs** | `test/golden_model_test.py` | Test edge cases: zero events, single event, all-zero timestamps, NaN coordinates, extreme coordinate values. Verify safety watchdog behavior for each. |
| 3 | **Add coverage instrumentation to CI** | `Makefile`, `.github/workflows/ci.yml` | Add `pytest-cov` for Python and `gcov` for C++ coverage reporting. Enforce minimum 80% line coverage on Python and C++ in CI. |
| 4 | **Write integration test README** | `test/README.md` | Document the full test hierarchy: unit → golden model → HIL. How to run specific tests. How to add a new scenario. The golden model equivalence approach. |

---

### Person 6 — DevOps/Infra Engineer (`@Enotrium/devops`)

> **Owns:** `.github/`, `Makefile`, `.gitignore`
> **Difficulty:** 🟡 Easy-Medium | **Commits:** 4

| # | Task | Files | Description |
|---|------|-------|-------------|
| 1 | **Add pre-commit hooks** | `.pre-commit-config.yaml`, `Makefile` | Add pre-commit config with `flake8`, `clang-format`, `yamllint`, and `trailing-whitespace` checks. Wire into `make lint`. Add setup instructions to `CONTRIBUTING.md`. |
| 2 | **Add Dockerfile for reproducible builds** | `Dockerfile`, `docker-compose.yml` | Containerize the full dev environment: Python 3.11 + PyTorch + g++-13 + Vitis HLS simulation libs. Add `make docker-build` and `make docker-test` targets. |
| 3 | **Add CI matrix for multiple Python versions** | `.github/workflows/ci.yml` | Extend CI to test against Python 3.10, 3.11, and 3.12. Ensure `setup.py` classifiers match. |
| 4 | **Add changelog enforcement to CI** | `.github/workflows/ci.yml`, `CHANGELOG.md` | Add CI check: PRs touching code (not docs-only) must also update `CHANGELOG.md`. Fail CI with a clear message if missing. |

---

### Person 7 — Technical Writer (`@Enotrium/docs`)

> **Owns:** All `*.md` files, `README.md`, `PRODUCTION_READINESS.md`
> **Difficulty:** 🟢 Easy | **Commits:** 4

| # | Task | Files | Description |
|---|------|-------|-------------|
| 1 | **Add full API reference for drone modules** | `drone/README.md` | Expand with complete class/method docs, parameter tables, return types, and minimal usage examples for all 9 drone modules (controller, object detector, collision predictor, evasion controller, TTC dense, event frame aggregator, direct action predictor, contrast maximizer, depth estimator). |
| 2 | **Add hardware setup guide** | New `HARDWARE_SETUP.md` | Step-by-step: which FPGA board to buy, how to flash the bitstream, how to wire the event camera (pin mappings), how to configure PX4 firmware, power delivery, ESC wiring. |
| 3 | **Update PRODUCTION_READINESS.md with real synthesis numbers** | `PRODUCTION_READINESS.md` | Once Person 1 completes synthesis, replace all theoretical resource/latency estimates with actual post-synthesis numbers. Add before/after comparison table. |
| 4 | **Add troubleshooting guide** | New `TROUBLESHOOTING.md` | Common issues and solutions: Vitis HLS license errors, CUDA version mismatch, MAVLink port conflicts, ARM cross-compilation gotchas, event camera driver issues, SD card logging failures. Link from `CONTRIBUTING.md`. |

---

### Person 8 — Security Engineer (`@Enotrium/security`)

> **Owns:** `SECURITY.md`
> **Difficulty:** 🟠 Medium-Hard | **Commits:** 3

| # | Task | Files | Description |
|---|------|-------|-------------|
| 1 | **Implement MAVLink v2 signing key management** | `arm/mavlink_bridge.h`, new `arm/secure_boot.h` | `SECURITY.md` documents MAVLink signing but it's not wired. Add actual signing key storage (eFUSE or encrypted flash), runtime key derivation, and periodic key rotation. |
| 2 | **Add FPGA bitstream integrity check on boot** | `arm/drone_main.cpp`, new `arm/secure_boot.h` | Read back FPGA configuration CRC via PCAP and compare against golden value before allowing arming. Part of the secure boot chain documented in `SECURITY.md` §4. |
| 3 | **Pen-test the HIL simulation** | `test/hil_gazebo_bridge.py`, `SECURITY.md` | Run each attack vector from `SECURITY.md` §3 (GPS spoofing, IMU injection, event camera blinding, MAVLink replay) in simulation. Document which defenses trigger and whether evasion still works. Commit a penetration test report to `SECURITY.md`. |

---

## Summary Matrix

| # | Person | Team | Difficulty | Commits | Files Touched |
|---|--------|------|------------|---------|---------------|
| 1 | FPGA Engineer | `@Enotrium/fpga` | 🔴 Hard | 4 | `fpga/*.h`, `fpga/build.tcl`, `fpga/testbench.cpp`, `fpga/README.md` |
| 2 | Embedded/ARM | `@Enotrium/embedded` | 🟠 Medium | 3 | `arm/drone_main.cpp`, `arm/*.h`, `arm/Makefile` |
| 3 | Autonomy | `@Enotrium/autonomy` | 🟠 Medium | 3 | `drone/*.py` |
| 4 | ML Engineer | `@Enotrium/ml` | 🔴 Hard | 4 | `models/*.py`, `train/*.py` |
| 5 | QA | `@Enotrium/qa` | 🟡 Easy-Medium | 4 | `test/*`, `Makefile`, `.github/` |
| 6 | DevOps | `@Enotrium/devops` | 🟡 Easy-Medium | 4 | `.github/`, `Makefile`, `Dockerfile` |
| 7 | Docs | `@Enotrium/docs` | 🟢 Easy | 4 | `*.md`, new `.md` files |
| 8 | Security | `@Enotrium/security` | 🟠 Medium-Hard | 3 | `arm/*`, `SECURITY.md` |

**Total: 29 discrete commits across 8 people. Zero file conflicts. Every task is scoped to one person's CODEOWNERS domain.**

---

## Order of Operations

Some tasks depend on others being completed first:

```
1. Person 6 (DevOps)     → Dockerfile + pre-commit     ← No dependencies
2. Person 7 (Docs)       → API ref, setup guide         ← No dependencies
3. Person 5 (QA)         → Regression tests + fuzzing   ← No dependencies
4. Person 2 (Embedded)   → Kalman + telemetry           ← No dependencies
5. Person 3 (Autonomy)   → SITL test + tuning           ← Needs Docker (Person 6)
6. Person 1 (FPGA)       → Synthesis + numbers          ← Needs hardware, independent
7. Person 4 (ML)         → Retrain + benchmark          ← GPU time, independent
8. Person 7 (Docs)       → Update PROD_READINESS        ← Needs Person 1's synthesis numbers
9. Person 8 (Security)   → Signing + bitstream + pen    ← Needs Person 2's integration
```

---

## Getting Started

```bash
# 1. Clone and branch
git clone git@github.com:Enotrium/FPGA-Event-Based-encode.git
cd FPGA-Event-Based-encode
git checkout -b your-initials/june10-task1

# 2. Do your task (see your section above)

# 3. Verify
make test        # Python + C++ + FPGA tests
make lint        # Code style

# 4. Commit and push
git add -A
git commit -m "june10: <short description of what you did>"
git push origin your-initials/june10-task1

# 5. Open a PR
# CODEOWNERS will auto-assign the correct reviewer
```

---

## Tracking

| Person | Task 1 | Task 2 | Task 3 | Task 4 | PRs Merged |
|--------|--------|--------|--------|--------|------------|
| FPGA Engineer | ⬜ | ⬜ | ⬜ | ⬜ | 0/4 |
| Embedded/ARM | ⬜ | ⬜ | ⬜ | — | 0/3 |
| Autonomy | ⬜ | ⬜ | ⬜ | — | 0/3 |
| ML Engineer | ⬜ | ⬜ | ⬜ | ⬜ | 0/4 |
| QA | ⬜ | ⬜ | ⬜ | ⬜ | 0/4 |
| DevOps | ⬜ | ⬜ | ⬜ | ⬜ | 0/4 |
| Docs | ⬜ | ⬜ | ⬜ | ⬜ | 0/4 |
| Security | ⬜ | ⬜ | ⬜ | — | 0/3 |

---

## IQT Due Diligence Readiness

> **The problem:** `git shortlog -sn` currently shows 1 committer. IQT technical due diligence runs this command as step one. A single-contributor repo with 9 CODEOWNERS teams and no multi-author PR history is a red flag — it looks like a solo founder with a padded org chart.

### What IQT Will Check

| Signal | What They Look For | Current State | 🚩 Level |
|--------|-------------------|---------------|----------|
| **`git shortlog -sn`** | Multiple authors with commits proportional to their role | 1 author, 100% of commits | 🔴 Critical |
| **PR merge history** | PRs opened by different users, merged by different users | All direct-pushed to `main` | 🔴 Critical |
| **Code review activity** | Different people reviewing each other's code in PR threads | No review history exists | 🔴 Critical |
| **GitHub team membership** | Do `@Enotrium/fpga`, `@Enotrium/ml`, etc. have real members visible on the org page? | Unknown — teams may be empty | 🔴 Critical |
| **Branch protection rules** | Is `main` protected? Require PRs? Require reviews? | No protection — direct push allowed | 🟠 High |
| **Contributor graph** | Green squares across multiple repos, not just one person | Likely 1 person across all repos | 🔴 Critical |
| **Cross-repo consistency** | Do other Enotrium repos show the same single-contributor pattern? | Likely yes | 🟠 High |
| **Org README / profile** | Does github.com/Enotrium list the team with roles and faces? | Unknown | 🟡 Medium |
| **Signed commits** | Are commits verified (GPG/SSH signed)? | Unknown | 🟡 Medium |
| **SBOM / dependency tracking** | `requirements.txt` frozen? `package-lock.json` present? | Yes (both present) | 🟢 Good |

### Immediate Org-Level Actions (Beyond JUNE10DO Tasks)

These must happen **before** the 29 code tasks or the multi-author commits will look staged:

| # | Action | Owner | Why IQT Cares |
|---|--------|-------|---------------|
| 1 | **Populate GitHub teams NOW** | CTO / Admin | Add real people to `@Enotrium/fpga`, `@Enotrium/embedded`, `@Enotrium/autonomy`, `@Enotrium/ml`, `@Enotrium/qa`, `@Enotrium/devops`, `@Enotrium/docs`, `@Enotrium/security`, `@Enotrium/systems`, `@Enotrium/core`. Teams must have visible members on https://github.com/orgs/Enotrium/teams before anyone opens a PR. |
| 2 | **Enable branch protection on `main`** | DevOps | Settings → Branches → Add rule for `main`: require pull request before merging, require 1 approval, dismiss stale reviews, require status checks (CI). This forces multi-author PR workflow and creates visible review history. |
| 3 | **Create `AUTHORS.md`** | Docs | List every team member with: full name, role, GitHub handle, email, domain expertise (e.g., "FPGA/Vitis HLS", "PyTorch/event cameras"). Include headshots if possible. Link from `README.md`. Humanizes the team — proves 8 real people exist. |
| 4 | **Everyone adds a verified email to GitHub** | Each person | Settings → Emails → Add and verify work email. Without this, commits may not attribute correctly and won't show on the contributor graph. |
| 5 | **Everyone configures GPG/SSH commit signing** | Each person | `git config --global commit.gpgsign true`. Verified commits show a green "Verified" badge — IQT checks this. In-Q-Tel's own standard requires signed commits. |
| 6 | **Audit all Enotrium repos for same pattern** | CTO | Run `git shortlog -sn` on every repo. If they all show 1 author, apply the same JUNE10DO pattern to each. Inconsistent contributor counts across repos = 🚩. |
| 7 | **Set up org-level README** | Docs | https://github.com/Enotrium should have an org README (`.github` repo) listing the mission, team, active repos, and links to key docs. This is the first thing IQT opens. |
| 8 | **Cross-link repos** | Docs | Every repo's README should link back to https://github.com/Enotrium and list its CODEOWNERS. Shows cohesive engineering org, not scattered solo projects. |
| 9 | **Everyone opens at least one PR, someone else reviews it** | All | Even a trivial docs change. The goal is visible PR → review → approve → merge history with different people as author, reviewer, and merger. The JUNE10DO tasks are designed for exactly this. |

### Timeline: What IQT Sees Before vs. After

| Artifact | Before (Today) | After (Post-Sprint) |
|----------|---------------|---------------------|
| `git shortlog -sn` | 1 name, 100% | 8+ names, proportional distribution |
| PR list | Empty (direct pushes) | 29+ merged PRs from 8 authors |
| Code review threads | None | Review comments from different people on each PR |
| GitHub teams | Possibly empty | 9 teams with visible members |
| Branch protection | Off | On — requires PR + review + CI pass |
| Contributor graph | 1 green tile | 8 overlapping green tiles |
| `AUTHORS.md` | Doesn't exist | Full team roster with roles |
| Commit verification | Unknown | All commits GPG/SSH signed |
| Org profile | Unknown | Complete with mission, team, repos |

### IQT-Specific Narrative

When the due diligence call happens, the story is:

1. **Team structure matches CODEOWNERS** — every subsystem has a named owner with domain expertise visible in their commit history
2. **Engineering process is real** — branch protection, PR reviews, signed commits, CI gating on every merge
3. **Not a solo founder** — 8 distinct people have contributed, reviewed, and merged code across hardware (FPGA), embedded (ARM), ML (PyTorch), autonomy (drone control), QA, DevOps, docs, and security
4. **Production discipline** — the JUNE10DO tasks close real gaps (synthesis numbers, HIL pens, adversarial robustness, ONNX export, secure boot integration) — not cosmetic changes
5. **Multi-repo consistency** — the same multi-author pattern exists across all Enotrium repos, not just the flagship FPGA repo

### Verification Script

Run this after the sprint to confirm IQT-readiness:

```bash
# Multi-author commit count
git shortlog -sn

# PR merge count by author
git log --merges --format='%an' | sort | uniq -c | sort -rn

# Signed commit ratio
echo "Signed: $(git log --format='%G?' | grep -c G)"
echo "Unsigned: $(git log --format='%G?' | grep -c N)"

# Files touched by each author
git log --format='%an' --name-only | sort | uniq -c | sort -rn | head -40

# Branch protection status (manual check)
open https://github.com/Enotrium/FPGA-Event-Based-encode/settings/branches

# Team membership visibility (manual check)
open https://github.com/orgs/Enotrium/teams
```
